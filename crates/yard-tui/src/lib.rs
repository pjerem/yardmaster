//! TUI dashboard (ratatui) attached to the daemon over IPC. Read-mostly;
//! every mutation goes through the daemon's gate engine.
//! See SPEC.md §CLI surface — `yard` with no arguments lands here.
